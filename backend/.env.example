# Copy this file to .env and fill in your keys.
# Never commit .env to Git — it is already in .gitignore.

# Tier 1 — Small model (optional: app still works without this)
OPENAI_API_KEY=sk-...

# Tier 2 — Premium model (optional: app still works without this)
ANTHROPIC_API_KEY=sk-ant-...
